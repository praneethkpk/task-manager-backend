spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/taskmanager_db}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:70133@Ppk}

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

  error:
    include-stacktrace: never

jwt:
  secret: ${JWT_SECRET:MySuperSecretKeyForTaskManagerApplication2026SuperSecureSecretKey}
  expiration: 86400000