spring:
  datasource:
    url: jdbc:mysql://localhost:3306/taskmanager
    username: your_username
    password: your_password

  jpa:
    hibernate:
      ddl-auto: update

jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000